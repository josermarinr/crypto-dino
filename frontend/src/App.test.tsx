import React from "react";
import { render, screen } from "@testing-library/react";
import App from "./App";

test("test of test", () => {
	const linkElement = "a";
	expect(linkElement).toBe("a");
});
